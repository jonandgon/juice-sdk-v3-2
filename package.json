{
  "name": "juice-sdk-v3",
  "version": "0.0.1-alpha",
  "description": "Juicebox SDK monorepo",
  "type": "module",
  "main": "index.js",
  "repository": "https://github.com/jonandgon/juice-sdk-v3-2",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "build:core": "yarn --cwd packages/core build",
    "build:react": "yarn --cwd packages/react build",
    "build": "yarn build:core && yarn build:react"
  }
}
